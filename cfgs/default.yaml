replay_buffer_enabled: True
expert_buffer_enabled: True
domain: sequential
benchmark_mode: continual
task_list:
  - block
  - button
  - door
  - peg
  - window
  - faucet

shuffle_tasks: false
seed: 0
num_envs: 10
vec_env_cls: SubprocVecEnv
single_task_name: null
gcrl: true
policy: SAC
rnd: true
cuda: true
torch_deterministic: true
learning_starts: 0

logging:
  project: RND ContinualBench
  enable_wandb: true
  wandb_entity: jivani-aahil-university-of-ottawa

train:
  episodes_per_task: 200

eval:
  eval_video_steps: 100000
  num_eval_episodes: 20